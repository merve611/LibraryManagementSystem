
<nav class="navbar navbar-expand-lg navbar-light bg-secondary">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Anasayfa</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">Hakkımızda</a>
                </li>
            </ul>
            @*Kullanıcı giriş yaptıysa Çıkış Yap butonunu, giriş yapmadıysa Giriş Yap butonunu ve Kayıt ol butonunu görecek*@
            @if (User.Identity.IsAuthenticated)
            {
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a asp-controller="Auth" asp-action="SignOut" class="btn btn-danger">Çıkış Yap</a>
                    </li>
                </ul>

            }else
            {
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a asp-controller="Auth" asp-action="SignLogin" class="btn btn-success">Giriş Yap</a>
                    </li>
                
              
                    <li class="nav-item">
                        <a asp-controller="Auth" asp-action="SignUp" class="btn btn-success">Kayıt Ol</a>
                    </li>
                </ul>

            }


        </div>
    </div>
</nav>